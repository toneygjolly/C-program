#include<stdio.h>
int main(){
    int n,i,f0=0,f1=1,fn;
    printf("enter tne number of sequence\n");
    scanf("%d",&n);
    printf("sequence:%d\n",n);
    for(i=0;i<n;i++){
        printf("%d,",f0);
        fn=f0+f1;
        f0=f1;
        f1=fn;
    }
    return 0;
}
